import { DataLink } from "./data"

export interface UserInfo {
  name: DataLink;
  description: DataLink;
  location: DataLink;
  university: DataLink;
  email: DataLink;
  github: DataLink;
  malt: DataLink;
  linkedin: DataLink;
}
